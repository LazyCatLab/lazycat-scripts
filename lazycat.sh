#!/bin/bash

# 标题部分
show_header() {
  RESET="\033[0m"
  BOLD="\033[1m"
  BLUE="\033[34m"

  echo -e "${BLUE}${BOLD}"
  echo "██╗      █████╗ ███████╗██╗   ██╗ ██████╗ █████╗ ████████╗"
  echo "██║     ██╔══██╗╚══███╔╝╚██╗ ██╔╝██╔════╝██╔══██╗╚══██╔══╝"
  echo "██║     ███████║  ███╔╝  ╚████╔╝ ██║     ███████║   ██║   "
  echo "██║     ██╔══██║ ███╔╝    ╚██╔╝  ██║     ██╔══██║   ██║   "
  echo "███████╗██║  ██║███████╗   ██║   ╚██████╗██║  ██║   ██║   "
  echo "╚══════╝╚═╝  ╚═╝╚══════╝   ╚═╝    ╚═════╝╚═╝  ╚═╝   ╚═╝   🐾🐾"
  echo
  echo "         懒猫实验室 · 一键部署脚本 v1.0"
  echo "         支持系统：Debian / Ubuntu"
  echo -e "${RESET}"
}

# 菜单展示
show_menu() {
  echo "------------------------"
  echo "1. 系统信息查询"
  echo "2. 系统更新"
  echo "3. 系统清理"
  echo "4. 测试脚本合集"
  echo "5. X-UI 安装"
  echo "------------------------"
  echo "0. 退出脚本"
  echo "------------------------"
}

# 功能选择
handle_selection() {
  case "$1" in
    1)
      echo "👉 正在收集系统信息..."
      uname -a
      lsb_release -a 2>/dev/null || cat /etc/os-release
      ;;
    2)
      echo "🔄 正在更新系统..."
      apt update && apt upgrade -y
      ;;
    3)
      echo "🧹 正在清理系统..."
      apt autoremove -y && apt autoclean -y
      ;;
    4)
      echo "🧪 测试脚本合集：功能暂未实现，可后续集成测速、跑分等"
      ;;
    5)
      echo "⚙️ 正在安装 X-UI..."
      bash <(curl -Ls https://raw.githubusercontent.com/vaxilu/x-ui/master/install.sh)
      ;;
    0)
      echo "👋 已退出懒猫脚本"
      exit 0
      ;;
    *)
      echo "❌ 无效输入，请输入 0-5 之间的数字"
      ;;
  esac
}

# 主流程
main() {
  clear
  show_header
  while true; do
    show_menu
    echo -n "请输入选项 [0-5]: "
    read choice
    handle_selection "$choice"
    echo
  done
}

main
